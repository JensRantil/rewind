=========
TODO list
=========
 * Make sure we have 100% coverage.
 * Add corruption detection of old batches in `logbook` at startup.
  * Add automatic data corruption correction based on the corruption
    detection.
 * Write tests that make sure all modules, public functions, classes and
   constants have pydocs.
 * Write tests that makes sure all parameters to functions and class
   methods are mentioned in the pydoc.
 * Add documentation about the overall architecture.
 * Write tests that makes sure imports are sorted in alphabetical order.
